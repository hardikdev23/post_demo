import '../index.dart';

class AppStrings {
  static ScreenTitle screenTitle = ScreenTitle();
  static Message message = Message();
  static ErrorMessage errorMessage = ErrorMessage();
}

class ScreenTitle {
  final String postListTitle = "Post List";
  final String postDetailsTitle = "Post Details";
}
